[Unit]
Description=Reload nginx container when cert is updated

[Path]
# ssl_update が nginx_rp への配布完了時に touch するファイル（ホスト側パス）
PathChanged=@@INSTALL_ROOT@@/ssl_share/nginx_rp/marker.updated
Unit=cert-reload.service

[Install]
WantedBy=default.target
