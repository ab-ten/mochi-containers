server {
  listen 443 ssl;
  server_name mochi.@@CERT_DOMAIN@@;

  ssl_certificate     /run/nginx-certs/_.@@CERT_DOMAIN@@.crt;
  ssl_certificate_key /run/nginx-certs/_.@@CERT_DOMAIN@@.key;

  root /usr/share/nginx/html;
  index index.html;
}
