[Unit]
Description=mail_service SMTP Proxy Socket (port @@MAIL_SMTP_PUBLIC_PORT@@)
Documentation=man:systemd.socket(5)

[Socket]
ListenStream=@@MAIL_SMTP_PUBLIC_PORT@@
Accept=false

[Install]
WantedBy=sockets.target
